# Price Tracker

Local-only app intended to run on a Macbook; no public deployment target.

## Make targets
```sh
make frontend-dev
make frontend-build
make frontend-lint
make backend-dev
make backend-test
make migrate
make verify-migrations
```
